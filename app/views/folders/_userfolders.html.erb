<% if current_user.folders != nil %>
  <ul>
  <% current_user.folders.each do |folder| %>
    <li>
      <%= link_to folder.name, folder_path(folder), class: "show_basket" %>
      <%= link_to "-", 
          folder_path(folder),
          method: :delete,
          value: folder.id,
          class: "delete_basket" %>
    </li>
  <% end %>
  </ul>
<% end %>
