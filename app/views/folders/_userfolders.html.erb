<% if current_user.folders != nil %>
  <ul>
  <% current_user.folders.each do |folder| %>
    <li><%= link_to folder.name, 
        folder_path(folder),
        class: "show_basket" %></li>
  <% end %>
  </ul>
<% end %>
